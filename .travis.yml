language: csharp
sudo: required
mono:
  - latest
  - 3.2.8

install: 
- sudo mozroots --import --machine --sync --url https://dxr.mozilla.org/mozilla-central/source/security/nss/lib/ckfw/builtins/certdata.txt

script: 
  - ./build.sh --target "Travis"
    

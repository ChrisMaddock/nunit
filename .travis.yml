language: csharp
sudo: false
mono:
  - latest
  - 3.2.8

install:
  - wget -q 'https://hg.mozilla.org/projects/nss/raw-file/tip/lib/ckfw/builtins/certdata.txt' -O "/tmp/certdata.txt" 
  - mozroots --import --ask-remove --file /tmp/certdata.txt

script: 
  - ./build.sh --target "Travis"
    
